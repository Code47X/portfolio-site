const theme = {
  fonts: {
    primary: "'Electrolize', sans-serif",
    secondary: "'Blinker', sans-serif"
  },

  colors: {
    primary: "#00ffff",
    aboutBg: "#444444"
  }
}

export default theme;
